/*<div>
  <label for="">Title</label>
  <input type="text" name="title" value="<%= book.title %>">
</div>
<div>
  <label>Authors</label>
  <select name="author" >
    <%authors.forEach(author =>{%>
    <%if (author.id=== book.author) {%>
      <option selected value="<%= author.name %>" label="<%= author.name%>"> 
  <% }else{%>
      <option value="<%= author.id %>" label="<%= author.name %>">
    <%}%>
  <%})%>
  </select>
</div>
<div>
  <label >Publish Date</label>
  <input type="date" name="publishedDate" value="<%= book.publishedDate == null ? '' : book.publishedDate.toISOString().split('T')[0] %>">
</div>
<div>
  <label >Page Count</label>
  <input type="number" name="pageCount" min='1'  value="<%=book.pageCount%>">
</div>



<div>
  <% if (!book.coverImageName) { %>
    <input type="file" name="cover" class="filepond">
  <% } else { %>
    <input type="file" name=".filepondcover" class="filepond">
    <script>
      const pond = FilePond.create(document.querySelector('.filepondCover'), {
        allowMultiple: false,
        instantUpload: false
      });
      pond.files = [
        {
          source: "uploads/bookCovers/<%= book.coverImageName %>",
          options: {
            type: 'local'
          }
        }
      ];
    </script>
  <% } %>
</div>










<div>
  <label>Description</label>
  <textarea name="description"><%=book.description%></textarea>
</div> */

